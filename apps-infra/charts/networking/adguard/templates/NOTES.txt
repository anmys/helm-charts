1. Get the application URL by running these commands:
{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}
  {{- range .paths }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}{{ .path }}
  {{- end }}
{{- end }}
{{- else if contains "NodePort" .Values.service.type }}
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "adguard.fullname" . }})
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo http://$NODE_IP:$NODE_PORT
{{- else if contains "LoadBalancer" .Values.service.type }}
     NOTE: It may take a few minutes for the LoadBalancer IP to be available.
           You can watch the status of by running 'kubectl get --namespace {{ .Release.Namespace }} svc -w {{ include "adguard.fullname" . }}'
  export SERVICE_IP=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ include "adguard.fullname" . }} --template "{{"{{ range (index .status.loadBalancer.ingress 0) }}{{.}}{{ end }}"}}")
  echo http://$SERVICE_IP:{{ .Values.service.webPort }}
{{- else if contains "ClusterIP" .Values.service.type }}
  kubectl port-forward --namespace {{ .Release.Namespace }} svc/{{ include "adguard.fullname" . }} {{ .Values.service.webPort }}:{{ .Values.service.webPort }}
  echo "Visit http://127.0.0.1:{{ .Values.service.webPort }} to use your application"
{{- end }}

2. AdGuard Home DNS Service:
   DNS Server: {{ include "adguard.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local
   External DNS: 192.168.1.2 (Raspberry Pi IP)
   
3. Default admin credentials:
   {{- if .Values.config.adguard }}
   Username: {{ .Values.config.adguard.username }}
   Password: {{ .Values.config.adguard.password }}
   {{- else }}
   Username: admin
   Password: admin
   {{- end }}
   
4. Accessing AdGuard Home:
   Web UI: https://{{ .Values.config.adguard.host }}
   DNS Server: 192.168.1.2
   
   Please change the default credentials upon first login. 